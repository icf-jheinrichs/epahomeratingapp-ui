import template from './search-filter-must-correct.html';
import controller from './search-filter-must-correct.controller';

let searchFilterMustCorrectComponent = {
    bindings : {
        registerFilter : '&'
    },
    template,
    controller,
    controllerAs : 'searchFilterMustCorrectCtrl'
};

export default searchFilterMustCorrectComponent;

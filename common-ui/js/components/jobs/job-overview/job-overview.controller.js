class jobOverviewController {
    constructor (
        $state,
        $stateParams
    ) {
        'ngInject';

        this.$state       = $state;
        this.$stateParams = $stateParams;
    }

    $onInit () {
    }
}

export default jobOverviewController;

import template from './diagnostics-page.html';
import controller from './diagnostics-page.controller';

let diagnosticsPageComponent = {
    template,
    controller,
    controllerAs : 'diagnosticsPageCtrl'
};

export default diagnosticsPageComponent;

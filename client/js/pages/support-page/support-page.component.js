import template from './support-page.html';
import controller from './support-page.controller';

let supportComponent = {
    template,
    controller,
    controllerAs : 'supportPageCtrl'
};

export default supportComponent;
